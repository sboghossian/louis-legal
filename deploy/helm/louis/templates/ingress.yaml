{{- if .Values.ingress.enabled -}}
{{- $fullName := include "louis.fullname" . -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: {{ $fullName }}
    labels:
        {{- include "louis.labels" . | nindent 8 }}
    {{- with .Values.ingress.annotations }}
    annotations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
spec:
    {{- with .Values.ingress.className }}
    ingressClassName: {{ . }}
    {{- end }}
    {{- with .Values.ingress.tls }}
    tls:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    rules:
        {{- range .Values.ingress.hosts }}
        - host: {{ .host | quote }}
          http:
              paths:
                  {{- range .paths }}
                  - path: {{ .path }}
                    pathType: {{ .pathType }}
                    backend:
                        service:
                            name: {{ $fullName }}-{{ .service }}
                            port:
                                name: http
                  {{- end }}
        {{- end }}
{{- end }}
